import 'package:flutter/material.dart';

/// Global navigator key for navigation without [BuildContext] (e.g. from services).
final GlobalKey<NavigatorState> appNavigatorKey = GlobalKey<NavigatorState>();
